-- SELECT * FROM sales
-- WHERE volume > 1000;


-- SELECT * FROM sales WHERE is_recurring = TRUE;

-- SELECT * FROM sales
-- WHERE (is_disputed = TRUE) AND (volume > 5000);

-- SELECT * FROM sales
-- WHERE (is_disputed = TRUE) OR (volume > 5000);